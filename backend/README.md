# Pre-Legal backend

This backend provides the sign-up and sign-in API required by the Pre-Legal app.

## Commands

- `uv sync`
- `uv run uvicorn app.main:app --reload`

The SQLite database is created in `backend/data/prelegal.sqlite` when the app starts.
